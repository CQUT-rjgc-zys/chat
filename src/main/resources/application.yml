server:
  port: 8080

# http://localhost:8080/index

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mychat?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: 123456
  mvc:
    view:
      prefix: /templates/
      suffix: .html
    resources:
      static-locations: classpath:/static/, classpath:/public/, classpath:/templates/

